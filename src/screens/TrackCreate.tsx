import React from "react";
import { View, Text } from "react-native";
import { BASE_URL } from "../config";
import { BASE_URL } from "../config";

export default function CreateScreen () {
    return (
    
    <View>
        <Text>Создание трека</Text>
    </View>

    )
}